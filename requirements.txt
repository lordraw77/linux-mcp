mcp[cli]>=1.8.0,<2
paramiko>=3.0.0
python-dotenv>=1.0.0
openai>=1.0.0
# SSE transport dependencies (also pulled in by mcp[cli], listed explicitly for clarity)
uvicorn[standard]>=0.30.0
starlette>=0.40.0
